[project]
name = "tac"
version = "0.0.1"
description = "A Python framework for building agentic applications with Twilio"
authors = [{ name = "Twilio Conversation AI", email = "team_conversational-ai@twilio.com" }]
readme = "README.md"
homepage = "https://github.com/twilio-internal/twilio-agentic-framework-python"
repository = "https://github.com/twilio-internal/twilio-agentic-framework-python"
requires-python = ">=3.9,<4"
classifiers = [
    "Typing :: Typed",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "pydantic>=2.0.0,<3",
    "requests>=2.31.0,<3",
    "httpx>=0.27.0,<1",
    "python-dotenv>=1.0.0,<2",
    "twilio>=9.8.3,<10",
]

[project.optional-dependencies]
dev = [
    # Testing
    "pytest>=7.0.0,<8",
    "pytest-asyncio>=0.21.0,<1",
    "pytest-cov>=5.0.0,<6",
    # Linting and formatting
    "ruff>=0.8.0,<1",
    "mypy>=1.0.0,<2",
    "types-requests>=2.31.0,<3",
    # Example dependencies
    "openai>=2.0.0",
    "openai-agents>=0.6.0",
    # Voice dependencies
    "fastapi>=0.115.0,<1",
    "uvicorn[standard]>=0.32.0,<1",
    # Bedrock dependencies
    "boto3>=1.34.0,<2",
]
voice = [
    "fastapi>=0.115.0,<1",
    "uvicorn[standard]>=0.32.0,<1",
]
bedrock = [
    "boto3>=1.34.0,<2",
]

[tool.uv.workspace]
members = ["tac"]

[tool.uv.sources]
tac = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tac"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "N",   # pep8-naming
]

[tool.ruff.lint.per-file-ignores]
"examples/**/*.py" = ["E402", "E501", "N803"]  # Allow import order, long lines, and Twilio parameter names in examples

[tool.ruff.lint.isort]
known-first-party = ["tac"]
combine-as-imports = true

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
docstring-code-format = true

[tool.mypy]
python_version = "3.9"
plugins = ["pydantic.mypy"]
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
explicit_package_bases = true
exclude = [
    "tac/tools/examples/.*",
    ".*/site-packages/mcp/.*"
]

# Specify the paths to type check
[[tool.mypy.overrides]]
module = ["src.tac.*", "twilio.*"]  # twilio.* added to ignore missing type stubs
ignore_missing_imports = true

# Bedrock examples use boto3 which lacks type stubs
[[tool.mypy.overrides]]
module = ["examples.agents.bedrock.*"]
ignore_errors = true

# Scripts and tools examples have Optional/complex typing issues
[[tool.mypy.overrides]]
module = ["examples.scripts.*", "examples.tools.*"]
ignore_errors = true

# exec_demo uses Python 3.10 syntax and local imports
[[tool.mypy.overrides]]
module = ["examples.exec_demo.*"]
ignore_errors = true

# Ignore mcp module to avoid Python 3.10 syntax errors when checking with 3.9
[[tool.mypy.overrides]]
module = "mcp.*"
ignore_errors = true
follow_imports = "skip"

# voice_escalation has complex OpenAI API overload issues
[[tool.mypy.overrides]]
module = ["examples.channels.voice_escalation"]
ignore_errors = true

# Relaxed rules for examples (allow untyped decorators like @app.post())
[[tool.mypy.overrides]]
module = ["examples.*"]
disallow_untyped_decorators = false

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q --strict-markers --strict-config --cov=src/tac --cov-report=term-missing --cov-report=html"
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "asyncio: mark test as an asyncio test",
]

[tool.coverage.run]
source = ["src/tac"]
omit = ["*/tests/*", "*/__pycache__/*", "*/.venv/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]
precision = 2
show_missing = true
